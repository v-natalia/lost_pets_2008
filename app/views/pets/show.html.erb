<div class="container">
  <h1><%= @pet.name %></h1>
  <h2><%= @pet.species %></h2>
  <h3><%= @pet.address %> - le <%= @pet.date.strftime("%d/%m/%Y") %></h3>
  <%= link_to 'Delete this pet', pet_path(@pet), data: {turbo_method: :delete, confirm: 'Sure?' } %>

</div>
